document.addEventListener('DOMContentLoaded',function(){const customMenu=document.getElementById('contextMenu');const content=document.querySelector('body');let isContextMenuOpen=false;content.addEventListener('contextmenu',function(event){event.preventDefault();customMenu.style.display='block';const scrollY=window.scrollY||window.pageYOffset;const menuWidth=customMenu.offsetWidth;const menuHeight=customMenu.offsetHeight;const windowWidth=window.innerWidth;const windowHeight=window.innerHeight;let x=event.clientX+'px';let y=event.clientY+scrollY+'px';if(event.clientX+menuWidth>windowWidth){x=windowWidth-menuWidth+'px'}if(event.clientY+menuHeight>windowHeight+scrollY){y=windowHeight-menuHeight+scrollY+'px'}customMenu.style.left=x;customMenu.style.top=y;isContextMenuOpen=true});customMenu.addEventListener('click',function(event){const menuItemId=event.target.id;switch(menuItemId){default:break;case 'menu-item-1':ToggleThemes();break;case 'menu-item-2':ToggleFontStyle();break;case 'menu-item-3':ToggleAnimations();break;case 'menu-item-4':OpenAdvancedSearchWindow();break}customMenu.style.display='none';isContextMenuOpen=false});customMenu.addEventListener('contextmenu',function(event){event.preventDefault()});document.addEventListener('click',function(event){if(isContextMenuOpen&&!customMenu.contains(event.target)){customMenu.style.display='none';isContextMenuOpen=false}})});